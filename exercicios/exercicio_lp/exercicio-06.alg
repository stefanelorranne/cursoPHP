algoritmo "Exercicio 06"
// Função :
// Autor :
// Data : 10/03/2023
// Seção de Declarações 
var
base, altura, area : inteiro
inicio
escreval("Qual é a base do seu retângulo? ")
leia(base)
escreval("Qual é a altura do seu retângulo? ")
leia(altura)
area:= base*altura
escreval("A área do retângulo é:",area)
fimalgoritmo
